@model EpicWareWeb.Models.User
@{
    EpicWareWeb.Models.Connection conn = new EpicWareWeb.Models.Connection();
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AddFriend</title>
</head>
<body>
    <h3>Defina a Força e Tag de Conecção</h3>
    <div>
@using (Html.BeginForm())
{ 
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    @Html.HiddenFor(model => model.userID)
    <table >
        <tr>
            <td>@EpicWareWeb.Views.User.User.forca</td><td>@Html.TextBoxFor(model => conn.strenght)</td>
        </tr>
        <tr>
            <td>@EpicWareWeb.Views.User.User.tagconn</td><td>@Html.DropDownList("tagSelect", (SelectList)ViewBag.tagConnections)</td>
        </tr>
        <tr>
            <td><button type="submit" class="btn btn-sucess">@EpicWareWeb.Views.User.User.adicionar_amigo</button></td>
        </tr>       
    </table>
}
       
    </div>
</body>
</html>
