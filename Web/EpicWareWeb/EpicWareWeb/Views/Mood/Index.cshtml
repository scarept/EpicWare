@*@model IEnumerable<EpicWareWeb.Models.Mood>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.moodID }) |
            @Html.ActionLink("Details", "Details", new { id=item.moodID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.moodID })
        </td>
    </tr>
}

</table>*@
